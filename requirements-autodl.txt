# AutoDL 环境专用依赖
# 如果使用PyTorch基础镜像，以下包已预装，不需要重复安装：
# - torch
# - torchvision
# - torchaudio
# - numpy (通常已包含)

# 核心依赖（必须安装）
transformers>=4.20.0
datasets>=2.0.0  # Hugging Face 数据集库 ⚠️ 重要！
dashscope>=1.10.0
scikit-learn>=1.0.0
pandas>=1.3.0
pyyaml>=5.4.0
tqdm>=4.62.0
backoff>=2.1.0

# 可选依赖
matplotlib>=3.4.0
seaborn>=0.11.0

# Jupyter (可选)
jupyter>=1.0.0
ipykernel>=6.0.0
